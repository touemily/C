#include <stdio.h>

int main()
{
    int x;

    printf("Type an integer: ");
    scanf(%d, &x);
    printf("integer %d\n", x);

    return(0);

    /*
    float x;

    printf("Type any number: ");
    scanf("%f", &x);
    printf("Integer %f\n", x);

    return(0);
    */
}
